# Stage G — Shadow self-directed curriculum campaign

This campaign consumes 500 diagnostic gap observations, clusters only exact
typed artifact requests, and ranks three source-module candidates. It selected
`source_derived_finite_statistics` because it covered 320 observed gaps and
had attributed source material plus 240 independent exercises. A shortcut
candidate with the same lexical coverage but no provenance or exercises was
blocked.

The selected module was independently checked on 120 source-derived exercises:
120/120 completed with replay verification. Its proposal receipt replayed and
tamper rejection passed. The immutable curriculum manifest was unchanged and
production authorizations remained zero.

This is a shadow campaign: validation does not promote the module into live
routing. The campaign corpus hash is
`ed7cbfb032c00e676ef34ce089bb21b160bb259254338a9c24248c7ed096ea99`.

Run:

```text
cargo run --quiet --bin curriculum_campaign_bench
```
