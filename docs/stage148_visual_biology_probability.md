# Stage 148 — Visual base-count tables to bounded biology/probability

This checkpoint composes coordinate-preserving visual tables with the
source-derived bounded DNA biology pack and its explicitly gated finite-
probability handoff.  The bridge requires an exact `base,count` header, all
four DNA bases, bounded non-negative counts, and `uniform_position` sampling
before authorization.  It does not infer RNA, sequence order, population
frequencies, or independence.

## Frozen corpus

- corpus: 240 independently authored coordinate-preserving tables
- supported / ambiguous / unsupported: 120 / 80 / 40
- corpus SHA-256: `1130a0806cc952a0312b6a100f87b61e4771fbb06a76dfb580358a1d27d0cf8e`

## Results

- exact decisions: **240/240**
- authorized supported routes: **120/120**
- replay verification: **240/240**
- tamper rejection: **240/240**
- false authorizations: **0**
- false denials: **0**

The biology artifact remains a composition artifact; the canonical internal
sequence is only a deterministic carrier for the validated composition
operation.  Probability authorization requires the separately stated uniform
position sampling policy.

Machine-readable report: `docs/stage148_visual_biology_probability.json`.
