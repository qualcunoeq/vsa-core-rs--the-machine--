# Stage 252 — shifted portfolio technical-language pressure

Stage 252 independently pressures the four selected portfolio frontends with
reordered clauses, paraphrased operation descriptions, parenthesized lists,
and irrelevant bracketed context. Every report is offered to every frontend;
only the complete route matching the declared semantic family may authorize.

Results:

* 800 cases: 400 supported, 200 ambiguous, and 200 unsupported;
* 800/800 exact terminal decisions;
* 400/400 supported authorizations;
* 3,200/3,200 frontend receipts replay-verified and tamper-rejected;
* 400/400 downstream receipts replay-verified and tamper-rejected;
* zero route leakage, false authorizations, or false denials;
* parent portfolio and live state remained unchanged.

The pressure run exposed and repaired one repeated structural issue: bracketed
finite-sequence extraction now selects the sequence associated with the
declared Möbius indexing anchor rather than an incidental bracketed example.
Finite probability list extraction similarly binds bracket contents directly,
so parenthesized list context cannot change the typed distribution.

Corpus hash:
`91eb799e0ae0b8c52c3eed40ab5a73605a473406e01a8cd25ed8ff16ca4a0a5c`
